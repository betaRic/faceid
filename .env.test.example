# Isolated PostgreSQL route-test database. The database name must start with faceid_rc_.
FACEID_TEST_DATABASE_URL=postgres://postgres@127.0.0.1:55432/faceid_rc_local

# PostgreSQL 18 binary and test-owned data directories.
FACEID_TEST_PG_BIN=
FACEID_TEST_PG_DATA=
